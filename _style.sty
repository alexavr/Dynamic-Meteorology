% {{{ TEXT
\usepackage{lineno}                 % нумерация строк
\usepackage{xcolor}					% Make managment of colors easier and flaxible
\usepackage{cmap}					% поиск в PDF
% \usepackage{fontspec}
\usepackage{tokcycle,etoolbox,fontspec}
    \setmainfont{Raleway}
    % \setmainfont[]{Fira Sans}
    % \setmathrm[]{FreeSerif}
\usepackage[english,russian]{babel}	% локализация и переносы
\usepackage{lipsum}
\usepackage{sectsty}
    \chapternumberfont{\Huge} 
    \chaptertitlefont{\Huge}
% \usepackage[T2A]{fontenc}			% кодировка
\usepackage[utf8]{inputenc}			% кодировка исходного текста
\usepackage{indentfirst}
\frenchspacing
\usepackage{setspace}				% междустрочный интервал
\onehalfspacing						% задаем глобально 1.5
\definecolor{vms}{HTML}{2874A6}    % Текст НФ отсутствовал, но был заменен лекциями ВМС
\definecolor{noone}{HTML}{E74C3C}    % Текст НФ отсутствовал, но еще не был заменен
\definecolor{done}{HTML}{27AE60}    % Текст НФ отсутствовал, но еще не был заменен
% }}} % TEXT

% {{{ PREAMBULE

\usepackage{fancyhdr}			% Creates headers and footers
\usepackage{enumerate}          %These two package give custom labels to a list
\usepackage[shortlabels]{enumitem}
% Creates the header and footer. You can adjust the look and feel of these here.
\pagestyle{fancy}
\fancyhead[l]{Гавриков А.В.}
\fancyhead[c]{Динамическая метеорология}
\fancyhead[r]{\today}
\fancyfoot[c]{\thepage}
\renewcommand{\headrulewidth}{0.2pt} %Creates a horizontal line underneath the header
\setlength{\headheight}{15pt} %Sets enough space for the header
% }}} PREAMBULE

% {{{ TEXTBOX
\usepackage[many]{tcolorbox}    	% for COLORED BOXES (tikz and xcolor included)
\definecolor{main}{HTML}{5989cf}    % setting main color to be used
\definecolor{sub}{HTML}{cde4ff}     % setting sub color to be used
\definecolor{warn}{HTML}{FF5733}    % setting sub color to be used

\newtcolorbox{warn}{
    fontupper = \color{white}, % font weight and size
    code={\onehalfspacing}, % line spacing
    % boxrule = 1.5pt,
    colframe = warn, % frame color
    colback = warn, % frame color
}
\newtcolorbox{info}{
    % fontupper = \bf\large, % font weight and size
    code={\onehalfspacing}, % line spacing
    boxrule = 1.5pt,
    colframe = black % frame color
}
\newtcolorbox{boxAA}{
    fontupper = \bf\large\color{white}, % font weight and size
    code={\onehalfspacing}, % line spacing
    boxrule = 1.5pt,
    colframe = red, % frame color
    colback = red, % frame color
}
\newtcolorbox{boxB}{
    fontupper = \bf\color{main}, % font color
    boxrule = 1.5pt,
    colframe = main,
    rounded corners,
    arc = 5pt   % corners roundness
}
\newtcolorbox{boxC}{
    colback = sub, % background color
    boxrule = 0pt  % no borders
}
\newtcolorbox{boxD}{
    code={\doublespacing},
    colback = sub, 
    colframe = main, 
    boxrule = 0pt, 
    toprule = 3pt, % top rule weight
    bottomrule = 3pt % bottom rule weight
}
\newtcolorbox{boxE}{
    colback = sub,
    enhanced,
    boxrule = 1.5pt, 
    colframe = white, % making the base for dash line
    borderline = {1.5pt}{0pt}{main, dashed} % add "dashed" for dashed line
}
\newtcolorbox{boxF}{
    colback = sub, 
    colframe = main, 
    boxrule = 0pt, 
    leftrule = 6pt % left rule weight
}
\newtcolorbox{boxG}{
    colback = sub, 
    colframe = main, 
    boxrule = 0pt, 
    toprule = 6pt % top rule weight
}
\newtcolorbox{boxH}{
    sharpish corners, % better drop shadow
    colback = sub, 
    colframe = main, 
    boxrule = 0pt, 
    toprule = 4.5pt, % top rule weight
    enhanced,
    fuzzy shadow = {0pt}{-2pt}{-0.5pt}{0.5pt}{black!35} % {xshift}{yshift}{offset}{step}{options} 
}
\newtcolorbox{boxI}{
    sharpish corners, % better drop shadow
    boxrule = 0pt,
    toprule = 4.5pt, % top rule weight
    enhanced,
    fuzzy shadow = {0pt}{-2pt}{-0.5pt}{0.5pt}{black!35} % {xshift}{yshift}{offset}{step}{options} 
}
%}}} $ TEXTBOX

% {{{ PDFPLOTS для автоматического создания плотов
\usepackage{pgfplots}
\pgfplotsset{width=10cm,compat=1.9}
    % \pdfplotsset{compat=1.18, width=10cm}
%}}} PDFPLOTS

% {{{ EQ
\usepackage[makeroom]{cancel} % для возможности вычеркивать слагаемые в уравнениях
\usepackage{amsmath}      % A library of many standard math expressions
    \numberwithin{equation}{chapter}
    \usepackage{mathtext} 				% русские буквы в формулах
    \usepackage{mathtools}% quard brackets

\usepackage{amssymb} % для каппы в уравнениях

\usepackage{tikz}
    \usetikzlibrary{arrows}
\newcommand\tikzmark[1]{\tikz[remember picture,overlay]\coordinate (#1);}

% % меняем шрифт только для чисел
% \newfontfamily{\digits}{Arial}
%     \XeTeXinterchartokenstate=1
%     \chardef\CharNormal=0
%     \makeatletter
%     % Test for old and new versions of the latex kernel
%     \ifx\e@alloc@intercharclass@top\@undefined
%         \chardef\CharBound=255
%     \else
%         \chardef\CharBound=\e@alloc@intercharclass@top
%     \fi
%     \makeatother
%     \newXeTeXintercharclass\CharNumbers
%     \XeTeXcharclass`0=\CharNumbers
%     \XeTeXcharclass`1=\CharNumbers
%     \XeTeXcharclass`2=\CharNumbers
%     \XeTeXcharclass`3=\CharNumbers
%     \XeTeXcharclass`4=\CharNumbers
%     \XeTeXcharclass`5=\CharNumbers
%     \XeTeXcharclass`6=\CharNumbers
%     \XeTeXcharclass`7=\CharNumbers
%     \XeTeXcharclass`8=\CharNumbers
%     \XeTeXcharclass`9=\CharNumbers
    
%     \XeTeXinterchartoks\CharNormal\CharNumbers={\begingroup\digits}
%     \XeTeXinterchartoks\CharBound\CharNumbers={\begingroup\digits}
%     \XeTeXinterchartoks\CharNumbers\CharNormal={\endgroup}
%     \XeTeXinterchartoks\CharNumbers\CharBound={\endgroup}
    
\newcommand\pd[2]{\displaystyle\frac{\partial #1}{\partial #2}}
\newcommand\td[2]{\displaystyle\frac{d #1}{d #2}}
\newcommand\fd[1]{\displaystyle\frac{\partial #1}{dt}+
                u\frac{\partial #1}{\partial x}+v\frac{\partial #1}{\partial y}+w\frac{\partial #1}{\partial z}}

\newcommand\rb[1]{\displaystyle \left( #1 \right) }
\newcommand\qb[1]{\displaystyle \left[ #1 \right] }
% 

\newcommand{\romans}[1]{\MakeUppercase{\romannumeral #1}}

% }}} % EQ

% {{{ PICS
\usepackage{graphicx}           % Use this package to include images
    \graphicspath{./pics/}
    \usepackage[export]{adjustbox}% http://ctan.org/pkg/adjustbox
\usepackage{tikz}
\usepackage{tikz-3dplot}
\usetikzlibrary{intersections,fadings,decorations.pathreplacing,3d}
\usepgfplotslibrary{patchplots}
\usetikzlibrary{patterns}
% }}} % PICS

\usepackage{tocloft}				% dots in the tableofcontents

